# GitHub + jsDelivr 图床 · 单图上传工具

## 功能
给一个本地图片路径，自动复制到仓库、git 提交并推送，返回 jsDelivr 公开链接，直接贴网站 `<img src>` 用。

## 用法
```bash
python 上传图片.py "C:/某路径/图片.png"
```
成功后在终端打印：
```
https://cdn.jsdelivr.net/gh/shaco805/cdn-images@main/图片.png
```

## 前置
1. 已在 GitHub 建好 public 仓库 `shaco805/cdn-images`（已完成）。
2. 同目录 `token.txt` 里放你的 **PAT**（Fine-grained，权限 Contents=Read and write，仅限 cdn-images 仓库）。
   - ⚠️ token.txt 已被 .gitignore 忽略，绝不会上传到 GitHub。
   - ⚠️ 之前贴到对话里那串 token 视为已泄露，必须已在 GitHub 后台 Revoke，这里只放新生成的。
3. 本地 git 已配置身份（commit 需要）：
   ```bash
   git config --global user.name "你的名"
   git config --global user.email "你的邮箱"
   ```

## 配置（改上传图片.py 顶部“配置”区）
- 仓库用户 / 仓库名 / 分支：对应你的仓库
- 图片子目录：留空放根目录；要分类填 "images" 等
- token文件名：默认 token.txt

## 安全提醒
- token 只存本地 token.txt，不进对话、不提交仓库。
- 任何密钥/密码都别贴进对话窗口。
